# Glow-Portfolio

Ilona van Oosbree's portfolio site, built with SvelteKit (Svelte 5) and Threlte for 3D.

## Developing

```sh
npm install
npm run dev -- --open
```

## Building

```sh
npm run build
```

Preview the production build with `npm run preview`.
